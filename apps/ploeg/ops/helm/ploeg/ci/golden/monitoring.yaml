---
# Source: ploeg/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: false

---
# Source: ploeg/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
  ports:
    - name: http
      port: 8080
      targetPort: http

---
# Source: ploeg/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ploeg
      app.kubernetes.io/instance: ploeg
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ploeg
        app.kubernetes.io/instance: ploeg
    spec:
      serviceAccountName: ploeg
      automountServiceAccountToken: false
      nodeSelector:
        node.webgrip.io/pool: worker
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ploegd
          image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PLOEG_LISTEN
              value: ":8080"
            - name: PLOEG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ploeg-db-app
                  key: uri
            - name: PLOEG_VIKUNJA_SECRET
              valueFrom:
                secretKeyRef:
                  name: ploeg-webhook-secret
                  key: PLOEG_VIKUNJA_SECRET
            - name: PLOEG_FORGEJO_URL
              value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
            - name: PLOEG_FORGEJO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agent-builder-token
                  key: FORGEJO_TOKEN
            - name: PLOEG_FORGEJO_BOT
              value: "agent-builder"
            - name: PLOEG_TARGET_FORGE
              value: "forgejo"
            - name: PLOEG_DEFAULT_TEAM
              value: "default"
            - name: PLOEG_LEASE_TTL
              value: "5m"
            - name: PLOEG_SWEEP_INTERVAL
              value: "15s"
            - name: PLOEG_TEAM_MAP
              value: ""
            - name: LITELLM_ADMIN_URL
              value: "http://litellm.ai.svc.cluster.local:4000"
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-litellm-master
                  key: LITELLM_MASTER_KEY
            - name: PLOEG_WORKER_AUTH_MODE
              value: "managed"
            - name: PLOEG_WORKER_SIGNING_KEY
              valueFrom:
                secretKeyRef:
                  name: ploeg-worker-signing
                  key: key
            - name: PLOEG_WORKER_BOOTSTRAPS
              valueFrom:
                secretKeyRef:
                  name: ploeg-worker-bootstrap
                  key: registry.json
            - name: PLOEG_WORKER_LLM_POLICIES
              value: "[]"
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 256Mi

---
# Source: ploeg/templates/prometheusrule.yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    release: kube-prometheus-stack
spec:
  groups:
    - name: ploeg
      rules:
        - alert: PloegShiftStuck
          expr: max by (team) (ploeg_shift_idle_seconds_max{job="ploeg",namespace="default"}) > 14400
          for: 15m
          labels:
            severity: warning
          annotations:
            summary: "A Shift of team {{ $labels.team }} has made no Run progress for over 4 hours"
            description: "No Run has started, finished or checkpointed on an open Shift for {{ $value | humanizeDuration }}. Lease and deadline renewals do not count as progress."
            runbook_url: https://docs.example/ploeg/ops/alerts/#ploegshiftstuck
        - alert: PloegLeaseExpired
          expr: max(ploeg_lease_overdue_seconds_max{job="ploeg",namespace="default"}) > 300
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: "A Run holds a Lease past its expiry"
            description: "The most overdue Lease expired {{ $value | humanizeDuration }} ago and the sweep has not released it."
            runbook_url: https://docs.example/ploeg/ops/alerts/#ploegleaseexpired
        - alert: PloegModelKeyPastTTL
          expr: max by (state) (ploeg_llm_key_ttl_overrun_seconds_max{job="ploeg",namespace="default"}) > 900
          for: 5m
          labels:
            severity: critical
          annotations:
            summary: "An inference account is still {{ $labels.state }} past its key TTL"
            description: "An account in state {{ $labels.state }} outlived its key TTL by {{ $value | humanizeDuration }}. Its gateway key may still be live."
            runbook_url: https://docs.example/ploeg/ops/alerts/#ploegmodelkeypastttl
        - alert: PloegSpendSpike
          expr: max(ploeg_settled_spend_usd_last_hour{job="ploeg",namespace="default"}) > 12.5
          for: 0m
          labels:
            severity: warning
          annotations:
            summary: "Settled spend in the last hour is above 12.5 USD"
            description: "Ploeg settled {{ $value | printf \"%.2f\" }} USD onto Shifts in the last hour."
            runbook_url: https://docs.example/ploeg/ops/alerts/#ploegspendspike
        - alert: PloegTrackerWebhookMissing
          expr: max(ploeg_tracker_webhooks_missing{job="ploeg",namespace="default"}) > 0
          for: 15m
          labels:
            severity: warning
          annotations:
            summary: "Tracker projects have no assignment webhook to Ploeg"
            description: "{{ $value }} configured Vikunja project(s) had no assignment webhook at the last check. Assignments there do not reach Ploeg."
            runbook_url: https://docs.example/ploeg/ops/alerts/#ploegtrackerwebhookmissing

---
# Source: ploeg/templates/servicemonitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ploeg
      app.kubernetes.io/instance: ploeg
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
