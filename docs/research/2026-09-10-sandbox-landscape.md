# Sandboxed coding agents: the landscape on 2026-09-10

Research date: 2026-09-10. Context: De Vloer runs `opencode serve` (HTTP with Basic authentication, OpenAPI at `/doc`, SSE at `/event`; [server docs](https://opencode.ai/docs/server/)) in a hardened Docker container on the workbench host or in a pod in a Kubernetes workspace namespace on Talos with Cilium and a Kata RuntimeClass. Every option below is judged against that seam. Verdicts are the researcher's; adoption decisions are recorded in the ADRs.

## Docker: Sandboxes, Docker Agent, Model Runner

Docker Sandboxes moved to microVMs on 2026-01-30 ([announcement](https://www.docker.com/blog/docker-sandboxes-run-claude-code-and-other-coding-agents-unsupervised-but-safely/)): a proprietary Docker VMM on Hypervisor.framework and WHP, the workspace mounted through virtiofs at the same absolute path or read-only at `/run/sandbox/source`, all outbound TCP through a host-side proxy with allow and deny lists and credential injection, and nested Docker inside the VM without host daemon access ([architecture](https://docs.docker.com/ai/sandboxes/architecture/)). Sandbox Kits, a YAML of tools, environment, allowed domains and startup commands, landed in March 2026 ([The New Stack](https://thenewstack.io/nanoclaw-docker-sandboxes-ai-agents/)). The docs describe an `sbx` CLI, cloud sandboxes, an MCP gateway and SSH into the sandbox for VS Code and Cursor ([docs](https://docs.docker.com/ai/sandboxes/)). Supported agents include Claude Code, Codex, Copilot, Gemini, Kiro, OpenCode and Docker Agent.

MicroVM sandboxes remain macOS and Windows only; Linux gets container sandboxes on Docker Desktop 4.57 and later ([blog, 2026-03-11](https://www.docker.com/blog/building-ai-teams-docker-sandboxes-agent/)), with Docker VMM on Linux "when GA" ([Cloud Native Now](https://cloudnativenow.com/features/docker-desktop-gets-a-hypervisor-of-its-own/)). Docker Agent (`cagent`) is a YAML multi-agent runtime distributed as OCI artifacts with an ACP server since 2025-11-13 and bundled in Docker Desktop 4.49 ([blog](https://www.docker.com/blog/cagent-comes-to-docker-desktop-with-built-in-ide-support-through-acp/), [releases](https://github.com/docker/docker-agent/releases)). Model Runner is GA and OpenAI-compatible with Apple silicon GPU support ([blog](https://www.docker.com/blog/announcing-docker-model-runner-ga/)). There is no REST, gRPC or SDK surface; third parties shell out to `docker sandbox create|run|exec`.

Verdict: the best on-workstation isolation on macOS today, and it already knows OpenCode, so De Vloer's docker placement could become `docker sandbox run` plus SSH. It is a black box with a closed VMM, no API and no Linux microVMs, so it belongs as one placement among several rather than a foundation.

## Apple `container`

Version 1.0.0 shipped on 2026-06-09 with container machines, persistent VM-like Linux environments ([WWDC26 session 389](https://developer.apple.com/videos/play/wwdc2026/389/)), TOML configuration and `container cp`; 1.2.1 added a Kubernetes plugin for a local cluster and `container export`; 1.4.1 on 2026-09-09 is current with Containerization 0.45.0 ([releases](https://github.com/apple/container/releases)). One lightweight VM per container, its own IP, OCI images, and a Swift API for images, storage, networking and init ([framework](https://github.com/apple/container)). Isolated networks need macOS 26; Sequoia has no container-to-container isolation ([guide](https://kalinga.ai/apple-container-guide-2026/)).

Verdict: Apple silicon only, Swift-native API, no Linux. A reasonable third local placement because each OpenCode gets a real kernel with sub-second boot and no Docker Desktop dependency. Networking policy is far weaker than Docker's proxy, so it needs a host proxy the workbench controls.

## Kubernetes-native

[kubernetes-sigs/agent-sandbox](https://github.com/kubernetes-sigs/agent-sandbox/releases) reached v1.0.0 on 2026-08-28 with only v1beta1, conversion webhooks removed, a sandbox-router with path routing and session cookies, a `sandboxd` daemon and Go, Python and TypeScript SDKs; v1.0.1 on 2026-09-03 added a TypeScript client and an OpenHands warm-pod binding. The v0.5 series added strict Sandbox to Pod mapping, WebSocket proxying through the router, a ServiceMonitor, and E2B `envd` and Pi-agent examples ([docs](https://agent-sandbox.sigs.k8s.io/)). Red Hat ships a build ([2026-07-15](https://developers.redhat.com/articles/2026/07/15/red-hat-build-agent-sandbox-isolated-workload-management-kubernetes)). Isolation is delegated through `runtimeClassName` to gVisor or Kata ([Firecracker example](https://agent-sandbox.sigs.k8s.io/docs/use-cases/examples/firecracker-sandbox/)).

User namespaces went GA in Kubernetes 1.36 on 2026-04-22 with `hostUsers: false` ([blog](https://kubernetes.io/blog/2026/04/23/kubernetes-v1-36-userns-ga/)); Sysbox 0.7.0 is compatible with containerd 2.0.5 and later ([releases](https://github.com/nestybox/sysbox/releases)). Kata 4.0.0 on 2026-07-20 rewrote the runtime in Rust as the default, built Dragonball in, added virtio-mem hotplug and EROFS with dm-verity; 4.1.0 followed on 2026-08-21 ([releases](https://github.com/kata-containers/kata-containers/releases)). The Talos extension bundles QEMU only; Cloud Hypervisor and Firecracker need a custom extension ([Talos guide](https://github.com/oneuptime/blog/tree/master/posts/2026-03-03-install-kata-containers-runtime-on-talos-linux)). gVisor publishes weekly tags with systrap as the default platform and nvproxy for GPUs ([agent-sandbox gVisor guide](https://agent-sandbox.sigs.k8s.io/docs/guides/gvisor/)).

`PortForwardWebsockets` graduated to stable in 1.35 (`v5.channel.k8s.io`, negotiated through `Sec-WebSocket-Protocol`; [Sysdig on 1.35](https://www.sysdig.com/blog/kubernetes-1-35-whats-new), [transition blog](https://kubernetes.io/blog/2024/08/20/websockets-transition)). A plain WebSocket client in a VS Code extension or a browser can port-forward to a pod with only a bearer token.

Verdict: the strongest fit. Sandbox, SandboxWarmPool and SandboxClaim are exactly De Vloer's Kubernetes placement with a stable API and a router that already proxies WebSockets. Kata 4 on Talos gives the hardware boundary; `hostUsers: false` hardens plain containers. WebSocket port-forward removes the last reason for an API server proxy detour.

## Hosted sandbox APIs

| Service | State | Open or self-hosted |
| --- | --- | --- |
| E2B | Firecracker fork; `e2b-dev/infra` Apache-2.0 with Terraform and Nomad on GCP, AWS in beta; client-proxy 1.0.0 on 2026-07-07 ([guide](https://www.agenticwire.news/article/e2b-self-hosted-guide)) | Yes, heavy; `envd` reused by agent-sandbox |
| Daytona | Closed source since June 2026; the public repository froze at v0.190.0 ([announcement](https://www.daytona.io/dotfiles/updates/daytona-is-going-closed-source)) | No longer |
| Modal | Filesystem, directory and memory snapshots, the last in alpha with seven-day expiry ([docs](https://modal.com/docs/guide/sandbox-snapshots)) | No |
| Vercel Sandbox | GA; persistence by default since 2026-05-26 ([changelog](https://vercel.com/changelog/sandbox-persistence-is-now-ga)) | No |
| Cloudflare | Containers and Sandbox SDK GA on 2026-04-13; Dynamic Workers in open beta since April 2026; outbound Workers for identity-aware egress ([changelog](https://developers.cloudflare.com/changelog/post/2026-04-13-containers-sandbox-ga/), [InfoQ](https://www.infoq.com/news/2026/04/cloudflare-dynamic-workers-beta/)) | workerd is open, the platform is not |
| Anthropic Managed Agents | Public beta 2026-04-08; self-hosted sandboxes in beta since 2026-05-19 with a pull-based work queue where a pod runs `ant beta:worker` and executes bash and file tools locally ([docs](https://platform.claude.com/docs/en/managed-agents/self-hosted-sandboxes), [agent-sandbox recipe](https://agent-sandbox.sigs.k8s.io/docs/use-cases/anthropic-managed-agents/)) | Execution self-hosted; orchestration remote |
| OpenAI Codex cloud | Ephemeral `codex-universal` containers, internet only during setup by default ([docs](https://developers.openai.com/codex/cloud/environments)) | No |
| Fly Sprites | January 2026; Firecracker VMs that hibernate, copy-on-write checkpoints in about 300 ms over REST, Python and Rust ([API](https://sprites.dev/api), [Willison](https://simonwillison.net/2026/Jan/9/sprites-dev/)) | No |
| Blaxel, Northflank | Perpetual sandboxes with sub-25 ms resume; Northflank offers bring-your-own-cloud microVMs ([Northflank](https://northflank.com/blog/best-sandboxes-for-coding-agents)) | Northflank BYOC only |

Verdict: nothing here replaces a self-hosted workbench, but two designs are worth adopting: Anthropic's pull-based tool-execution queue, where the sandbox only needs outbound HTTPS, and Sprites' checkpoint-as-evidence model. E2B's `envd` is the one open in-sandbox daemon with an established API.

## MicroVM tooling for self-hosting

Firecracker is active (the CVE-2026-1386 jailer fix; [releases](https://github.com/firecracker-microvm/firecracker/releases)). Cloud Hypervisor v53.0 on 2026-07-12 added an offloaded snapshot and restore daemon ([blog](https://www.cloudhypervisor.org/blog/cloud-hypervisor-v53.0-released/)). libkrun releases monthly ([releases](https://github.com/libkrun/libkrun/releases)) and powers Podman machine and containerd `nerdbox`. microsandbox v0.6.15 on 2026-08-24 builds on libkrun with Rust, Python, TypeScript and Go SDKs, OCI images and sub-100 ms boot; `msbd` exposes it over REST ([repo](https://github.com/superradcompany/microsandbox), [msbd](https://github.com/mark3labs/msbd)). NVIDIA OpenShell v0.0.115 on 2026-08-28 is an alpha runtime with YAML policies over files, network, processes and inference calls plus a k3s gateway, co-maintained by Red Hat for Claude self-hosted sandboxes ([repo](https://github.com/NVIDIA/OpenShell), [Red Hat](https://www.redhat.com/en/blog/bringing-claude-self-hosted-sandboxes-to-openshell-on-red-hat-ai)). Unikraft v0.21.0 and KraftKit v0.12.14 give 10 ms sandboxes ([kraftkit](https://github.com/unikraft/kraftkit/releases)); WASI 0.3 with native async arrived in June 2026 ([Cosmonic](https://cosmonic.com/blog/ai-sandbox-guide/)). Neither runs an unmodified OpenCode with a toolchain.

Verdict: on a Talos cluster Kata 4 is the sane choice; microsandbox is the interesting workstation alternative to Docker Sandboxes because it is open, scriptable and cross-platform. OpenShell is the one to watch for policy as YAML over inference calls.

## Agent-side protocols and IDE hosting

ACP v1 is stable (SDK 1.4.0) and its registry passed 50 agents by June 2026. The v2 draft of 2026-07-20 covers sessions beyond a single turn, stable-ID patching of messages, tool calls and terminals, structured file operations with git-patch output and extensible permission subjects ([announcement](https://agentclientprotocol.com/announcements/acp-v2-draft)). It remains stdio-first, and Zed's remote ACP has open issues ([#47910](https://github.com/zed-industries/zed/issues/47910), [#52254](https://github.com/zed-industries/zed/issues/52254)). OpenCode ships `opencode acp` ([docs](https://opencode.ai/docs/acp/)).

VS Code's Agent Host and the Agent Host Protocol (blog 2026-08-26, shipped in 1.136 on 2026-09-02) introduce a standalone process owning sessions, JSON-RPC, URI-addressed channels for sessions, chats, terminals and changesets, multi-client synchronisation, and remote hosts over SSH or dev tunnels exposing AHP over WebSocket, with an MIT specification and Rust, TypeScript, Kotlin, Go, Swift and .NET SDKs ([blog](https://code.visualstudio.com/blogs/2026/08/26/agent-host-architecture), [spec](https://microsoft.github.io/agent-host-protocol/), [repo](https://github.com/microsoft/agent-host-protocol), [1.136 notes](https://code.visualstudio.com/updates/v1_136)). Harnesses are hardcoded to Copilot and Claude; an extension API to register third-party hosts is an open, unassigned issue ([#325827](https://github.com/microsoft/vscode/issues/325827)). A community OpenCode AHP plugin exists as a skeleton ([repo](https://github.com/maxious/opencode-plugin-agent-host-protocol)). Cursor's Cloud Agents API v1 is in public beta and requires GitHub ([docs](https://cursor.com/docs/cloud-agent/api/endpoints)); Zed 1.0 shipped on 2026-04-29 with SSH remoting.

Verdict: AHP is the first state-first, multi-client, remote-native session protocol, which is precisely what an operator workbench needs; ACP v2 converges on the same session model. The gap is that VS Code will not let an extension register a foreign host yet, so De Vloer's extension keeps its own UI while the server speaks AHP.

## Evidence and handoff

Kubernetes ContainerCheckpoint remains beta on the kubelet API only; 1.37 adds beta CRI pod-checkpoint RPCs ([summary](https://cicd.deployment.to/kubernetes-1-37-beta-statefulset-checkpoint-cri/)); EKS packages CRIU checkpoints as OCI images ([AWS](https://aws.amazon.com/blogs/containers/forensic-container-checkpointing-on-amazon-eks/)). Kata VMs need hypervisor snapshots instead. Agent Trace, Cursor's RFC from January 2026 and on the Thoughtworks Radar, maps code ranges to conversations as JSON records ([repo](https://github.com/cursor/agent-trace/blob/main/README.md), [InfoQ](https://www.infoq.com/news/2026/02/agent-trace-cursor/)). SLSA v1.2 adds the Source Track ([spec](https://slsa.dev/spec/v1.2/)); gitsign 0.16.0 on 2026-05-06 signs commits keylessly with Rekor ([releases](https://github.com/sigstore/gitsign/releases)); sigstore-a2a signs agent cards with SLSA provenance ([repo](https://github.com/sigstore/sigstore-a2a)); Red Hat writes on agent identity provenance ([2026-08-07](https://next.redhat.com/2026/08/07/supply-chain-provenance-for-ai-agent-identity/)). jj workspaces are the emerging per-agent working-copy pattern ([geirsson](https://geirsson.com/jj-workspaces)).

Verdict: nobody yet combines these; De Vloer's candidate handoff is the natural carrier.

## Ranked shortlist for this workbench

1. **AHP-native workbench over WebSocket port-forward.** Expose each workspace's OpenCode as an AHP host with sessions, chats, terminals and changesets channels, and let the VS Code extension, a browser and a second operator attach to the same session, tunnelled through `v5.channel.k8s.io` with only a ServiceAccount token. Feasible now: the SDKs are MIT and the OpenAPI and SSE surface of `opencode serve` maps cleanly. Risk: AHP is weeks old and unversioned.
2. **Sandbox CRD placement with Kata 4 warm pools and evidence checkpoints.** Replace the hand-rolled pod with agent-sandbox v1.0: a SandboxWarmPool of Kata pods on Talos, a SandboxClaim per session, the sandbox-router for the WebSocket. At candidate time, take a VM snapshot and store it as an OCI artifact next to the git bundle. Feasible: the CRDs are stable; VM snapshotting on Talos needs a Kata extension that bundles Cloud Hypervisor.
3. **Signed, traced candidate bundles.** Agent commits signed under a per-session short-lived identity, a SLSA v1.2 source-track attestation for the bundle, and an Agent Trace record linking every hunk to the OpenCode session. Feasible immediately; nothing like this ships anywhere yet.
4. **Pull-based tool execution for local placements.** Mirror Anthropic's work-queue design so the sandbox needs only outbound HTTPS to De Vloer; a Docker Sandbox microVM, an Apple container machine or a Kata pod behind NAT become interchangeable and the inbound Basic-auth listener goes away.
5. **microsandbox on libkrun as the open cross-platform local runtime.** Drive it through `msbd` or the TypeScript SDK for the same experience on Linux and macOS without Docker Desktop, with Docker Sandboxes as the polished fallback and `hostUsers: false` for plain containers. The project is young and KVM-only on Linux.
