# C4 views: from the reason to the running software

Vloer provides human interaction and workspace execution; Ploeg authorizes shared sessions and coordinates unattended work. Local work must remain usable without Ploeg. Whether both applications should use a common runner remains an [open implementation choice](questions.md). Product diagrams show proposals; deployment diagrams record the 11 September inspection. Repository-free conversation is not current session behavior.

C4 uses different zoom levels: software systems, independently running applications or stores, components inside an application, and selected code detail. Its word “container” means an application or data store; it does not mean every box is a Docker container. Infrastructure belongs in a separate deployment view. See [C4 diagrams](https://c4model.com/diagrams), [containers](https://c4model.com/diagrams/container), and [deployment views](https://c4model.com/diagrams/deployment).

Every diagram names its scope and state. Rectangles identify their type in the label. Arrows state an interaction. Dashed arrows marked “proposed” identify future behavior, not an existing integration. These are C4 views expressed in Mermaid notation, with supporting sequence diagrams; they are not new services to deploy.

## 1. System landscape — intended responsibilities

**Audience:** everyone. **Question:** who participates, and why does each system exist? **State:** intended product boundary, with proposed automation identified.

```mermaid
flowchart TB
    person["Developer or reviewer<br/>Person"]
    vloer["Vloer<br/>Software system<br/>Work with AI and inspect results"]
    ploeg["Ploeg<br/>Software system<br/>Manage AI work and its execution"]
    tracker["External ticket system<br/>Software system<br/>Requested work and priorities"]
    forge["Forge and CI<br/>Software systems<br/>Code, changes, and checks"]
    gateway["LiteLLM<br/>Software system<br/>Model access and usage records"]
    provider["Model provider<br/>External software system<br/>Generate model responses"]
    identity["Identity provider<br/>Software system<br/>Sign people in"]
    person -->|Talk, work, steer, and review| vloer
    vloer -->|Submit work and interventions| ploeg
    ploeg -->|Return activity, cost, and results| vloer
    vloer -->|Read and link tickets| tracker
    tracker -->|Supply assigned work| ploeg
    vloer -->|Authenticate people| identity
    ploeg -->|Arrange repository work and checks| forge
    ploeg -->|Authorize model access for its workers| gateway
    gateway -->|Request inference| provider
    forge -.->|Proposed: CI failures trigger bounded repair| ploeg
```

This view deliberately leaves machines and scaling out. “Arrange repository work” includes work done by Ploeg-managed executors; it is a system-level responsibility, not a claim that the controller process edits files. Detailed provider traffic appears in view 10.

## 2. Vloer system context — intended

**Audience:** product and engineering. **Question:** what must Vloer own? **State:** working direction from the domain discussion.

```mermaid
flowchart LR
    human["Developer / reviewer<br/>Person"]
    v["Vloer<br/>System in focus<br/>Human interaction and presentation"]
    p["Ploeg<br/>External to Vloer<br/>AI execution, activity, and results"]
    t["Ticket system<br/>External system"]
    i["Identity provider<br/>External system"]
    human -->|Discuss, edit, interrupt, inspect, accept| v
    v -->|Start AI work, send input, request stop| p
    p -->|Stream progress and return results| v
    v -->|Browse and link existing work| t
    v -->|Sign in| i
```

An open conversation can start without a ticket or repository. If the conversation becomes implementation work, the needed target and acceptance conditions must become explicit. That transition is not fully modeled or implemented yet. Vloer can have a server for authentication, account connections, and presentation; “does no execution” does not require it to be static HTML.

## 3. Ploeg system context — intended

**Audience:** product and platform engineers. **Question:** what inputs can start work, and what must Ploeg arrange? **State:** working direction; general agent and CI inputs are proposed.

```mermaid
flowchart TB
    v["Vloer<br/>Human interface"]
    t["Ticket system<br/>External requests"]
    c["CI<br/>Check outcomes"]
    a["Other agent<br/>Software participant"]
    p["Ploeg<br/>System in focus<br/>Own work state and execution decisions"]
    e["Executor and harness<br/>Execution software"]
    m["LiteLLM<br/>Model access controls"]
    f["Forge / result destination<br/>External system"]
    v -->|Conversation or instructed work| p
    t -->|Assigned ticket| p
    c -.->|Proposed: eligible failure| p
    a -.->|Proposed: authorized request for work| p
    p -->|Start, steer, and stop authorized execution| e
    e -->|Report activity and result| p
    p -->|Issue and revoke bounded model access| m
    p -->|Arrange result delivery under policy| f
```

Human and non-human participants share work identifiers and execution rules. They need not use the same screen. A future agent API must identify its caller, its authority, and the work it may affect; agent-to-agent communication is not established merely by drawing an arrow.

## 4. Vloer containers — implemented shared mode

**Audience:** developers. **Question:** what actually runs today? **State:** De Vloer rc.14 with Ploeg shared execution enabled.

```mermaid
flowchart TB
    subgraph vboundary["De Vloer application boundary — current"]
        browser["Browser application<br/>Container: HTML / JavaScript<br/>Display and send user actions"]
        server["Workbench server<br/>Container: Node / TypeScript<br/>Sessions, execution, and workspace management"]
        db[("Session store<br/>Container: SQLite<br/>Activity, permissions, and internal records")]
        files[("Saved results<br/>Container: files<br/>Patches, bundles, and manifests")]
        workspace["Agent workspace<br/>Container: OpenCode + relay + tools<br/>Repository and native session state"]
    end
    p["Ploeg API<br/>External system"]
    k["Kubernetes API<br/>Infrastructure service"]
    l["LiteLLM<br/>External system"]
    f["Forgejo<br/>External system"]
    browser -->|HTTPS commands and event stream| server
    server -->|Read and persist| db
    server -->|Save and serve evidence| files
    server -->|Authorize work, commands, and accounting| p
    server -->|Create and release workspace resources| k
    workspace -->|Outbound relay connection| server
    server -->|Harness commands over the relay| workspace
    workspace -->|Clone repository| f
    workspace -->|Inference with limited key| l
    server -->|Brief check with execution model access| l
```

The workspace and model calls are the main differences from the intended Vloer boundary. The browser is replaceable; the current server is required to coordinate this execution path. Source: [composition](../../apps/vloer/src/main.ts), [engine](../../apps/vloer/src/engine.ts), [Kubernetes resources](../../apps/vloer/src/runtime/kubernetes.ts), [relay](../../apps/vloer/src/runtime/relay.ts), and [store](../../apps/vloer/src/store.ts).

## 5. Ploeg containers — implemented

**Audience:** developers and operations. **Question:** how does unattended work differ from Vloer-managed work? **State:** current source and homelab configuration; bronze autoscaling is paused for the Vloer pilot.

```mermaid
flowchart TB
    t["Ticket system / forge<br/>External systems"]
    v["Vloer server<br/>External delegated executor"]
    subgraph pboundary["Ploeg application boundary — current"]
        controller["ploegd<br/>Container: Go service<br/>Claims, lifecycle, budgets, and API"]
        database[("Ploeg records<br/>Container: PostgreSQL<br/>Work, runs, leases, and accounting")]
        worker["ploeg-worker + harness<br/>Container: worker process<br/>Claim, execute, and report"]
    end
    keda["KEDA<br/>External scaling controller"]
    kube["Kubernetes API / Job controller<br/>Infrastructure"]
    lite["LiteLLM<br/>External model gateway"]
    t -->|Configured webhooks| controller
    v -->|Operator execution API| controller
    controller -->|Read and transact| database
    keda -->|SQL count of eligible work| database
    keda -->|Create worker Jobs| kube
    kube -->|Start configured worker process| worker
    worker -->|Claim, renew, checkpoint, report| controller
    controller -->|Manage per-execution keys and spend| lite
    worker -->|Inference using limited key| lite
```

KEDA currently queries PostgreSQL directly for its scaling signal; it does not claim the work. Worker claims go through Ploeg. Vloer's own workspace Pods are not those KEDA Jobs. Sources: [controller](https://forgejo.webgrip.dev/webgrip/ploeg/src/commit/023c29fd8380ac1d0cfa375fe0098e99006fc2e8/cmd/ploegd/main.go), [worker](https://forgejo.webgrip.dev/webgrip/ploeg/src/commit/023c29fd8380ac1d0cfa375fe0098e99006fc2e8/pkg/worker/worker.go), and [ScaledJob template](https://forgejo.webgrip.dev/webgrip/ploeg/src/commit/023c29fd8380ac1d0cfa375fe0098e99006fc2e8/ops/helm/ploeg/templates/scaledjob.yaml).

## 6. Vloer server components — implemented

**Audience:** engineers investigating the split. **Question:** which responsibilities are still coupled inside Vloer? **State:** source implementation, with the optional delivery service separated in view 11.

```mermaid
flowchart TB
    subgraph nodeprocess["One Vloer server process"]
        http["HTTP and identity<br/>Component"]
        tasks["Ticket readers and binding<br/>Component"]
        engine["Session engine<br/>Component<br/>Sequence roles and interventions"]
        authority["Ploeg client<br/>Component<br/>Admission and execution commands"]
        runtime["Runtime adapter<br/>Component<br/>Translate harness operations"]
        workspace["Workspace manager<br/>Component<br/>Prepare and release environment"]
        relay["Pull relay<br/>Component<br/>Reach the isolated harness"]
        store["Store and event log<br/>Component"]
        results["Candidate capture<br/>Component<br/>Retain changes and evidence"]
    end
    http -->|Read / import ticket| tasks
    http -->|Start / pause / answer / cancel| engine
    engine -->|Get and confirm authority| authority
    engine -->|Provision| workspace
    engine -->|Run role| runtime
    runtime -->|Native HTTP over relay| relay
    engine -->|Persist transitions and evidence| store
    engine -->|Capture result| results
    http -->|Read history and results| store
```

These boxes are modules, not microservices. Consolidating execution would concern the engine, runtime, workspace, and parts of result handling; it does not imply moving the human UI into Ploeg. Exact files and connections are listed in the [implementation inventory](../../apps/vloer/docs/research/2026-09-11-ecosystem-implementation.md#c4-components-inside-de-vloer).

## 7. Ploeg controller components — implemented

**Audience:** backend engineers. **Question:** what rules does Ploeg add beyond a queue and an autoscaler? **State:** current implementation.

```mermaid
flowchart TB
    subgraph goprocess["One ploegd process"]
        api["HTTP API and scoped identity<br/>Component"]
        ingest["Tracker and forge adapters<br/>Component"]
        routing["Target and routing rules<br/>Component"]
        lifecycle["Work and review lifecycle<br/>Component"]
        operator["Operator execution service<br/>Component"]
        llm["Model credential service<br/>Component"]
        recovery["Expired-work recovery<br/>Component"]
        store["Transactional store<br/>Component"]
        delivery["Candidate delivery records<br/>Component"]
    end
    api -->|Validate and normalize events| ingest
    ingest -->|Resolve team and target| routing
    routing -->|Create or update work| lifecycle
    api -->|Admit and command shared work| operator
    operator -->|Authorize model access| llm
    api -->|Worker claims and outcomes| lifecycle
    lifecycle -->|Persist work and ownership| store
    operator -->|Persist execution and command state| store
    llm -->|Persist account and spending state| store
    recovery -->|Resolve expired ownership| store
    api -->|Verify delivery identities and record transitions| delivery
    delivery -->|Persist acceptance barriers| store
```

Ploeg's “Run” and Vloer's role run are not one object: one current Ploeg operator Run covers a Vloer execution containing multiple sequential role runs. Do not equate their counts in telemetry. Source details are in the [implementation inventory](../../apps/vloer/docs/research/2026-09-11-ecosystem-implementation.md#c4-components-inside-ploeg) and [shared execution contract](../../apps/vloer/docs/contracts/ploeg-execution.md).

## 8. Selected code boundary — implemented

**Audience:** engineers assessing replacement. **Question:** what abstractions let a different harness or workspace fit? **State:** selected interfaces, not a complete class model.

```mermaid
flowchart LR
    engine["Engine<br/>src/engine.ts"]
    types["AgentRuntime / Workspace contracts<br/>src/types.ts"]
    opencode["OpenCode adapter<br/>src/runtime/opencode.ts"]
    command["Command adapter<br/>src/runtime/command.ts"]
    kubernetes["Kubernetes backend<br/>src/runtime/kubernetes.ts"]
    docker["Docker backend<br/>src/runtime/docker.ts"]
    authority["Execution authority client<br/>src/execution-authority.ts"]
    engine -->|Uses runtime and workspace shapes| types
    opencode -->|Implements runtime operations| types
    command -->|Implements runtime operations| types
    kubernetes -->|Produces and manages workspace| types
    docker -->|Produces and manages workspace| types
    engine -->|Shares authorization with Ploeg| authority
```

This is the useful code-level view for the present discussion. The [types](../../apps/vloer/src/types.ts), [OpenCode adapter](../../apps/vloer/src/runtime/opencode.ts), and [authority client](../../apps/vloer/src/execution-authority.ts) are the actual contracts. Adding a compatible adapter does not prove native conversation transfer or equivalent permissions. A future consolidation may move these contracts; that is not implied by this current-code view.

## 9. Start an imported ticket — observed path with a known failure

**Audience:** developers and testers. **Question:** what happens between clicking Start and receiving an agent action? **State:** current cluster path; manual permission handling has an unresolved defect.

```mermaid
sequenceDiagram
    actor Person
    participant V as Vloer
    participant T as Ticket system
    participant P as Ploeg
    participant K as Kubernetes
    participant W as Workspace / harness
    participant L as LiteLLM
    Person->>V: Select external ticket
    V->>T: Read ticket snapshot
    V->>P: Find eligible existing work and target
    V-->>Person: Preview what will run
    Person->>V: Start with allowed budget and model
    V->>P: Claim and authorize shared execution
    P->>L: Establish limited model access
    P-->>V: Execution identity and limited access
    V->>K: Create workspace resources
    K-->>W: Start repository clone and harness
    W->>V: Connect outbound relay
    V->>W: Submit role instructions
    W->>L: Ask model and execute allowed tools
    W-->>V: Activity, questions, and results
    V-->>Person: Show durable activity
```

The ticket test reached tool-permission requests, then OpenCode returned an invalid permission-list response and the adapter failed the run. The automatic-approval coding fixture completed separately. The diagram is not evidence that every permission interaction passed. Sources: [task binding](../../apps/vloer/docs/contracts/ploeg-tracker-binding.md), [engine](../../apps/vloer/src/engine.ts), and [runtime defect evidence](../../apps/vloer/docs/research/2026-09-11-ecosystem-implementation.md#runtime-evidence-and-current-limits).

## 10. One model request — authority and traffic are different

**Audience:** security, finance, and engineering. **Question:** where do requests, credentials, and cost records go? **State:** current shared execution pattern; Fireworks is intended but not configured in the homelab test.

```mermaid
sequenceDiagram
    participant P as Ploeg
    participant L as LiteLLM
    participant E as Executor / harness
    participant M as Model provider
    participant V as Vloer
    P->>L: Create limited execution key
    L-->>P: Key identity and constraints
    P-->>E: Supply only execution access through executor setup
    E->>L: Model request using limited key
    L->>M: Route request with provider credential
    M-->>L: Response and usage
    L-->>E: Model response
    P->>L: Read available spending records
    L-->>P: Observed usage and cost
    P-->>V: Execution accounting status
    P->>L: Block access when execution ends
```

Ploeg determines authorization. LiteLLM supplies gateway enforcement and estimates. The provider supplies inference and eventual billing. None is a guarantee of an exact cost ceiling for requests already in flight. Source: [credential ownership](https://forgejo.webgrip.dev/webgrip/ploeg/src/branch/development/docs/adrs/0025-management-authority-stays-in-the-control-plane.md), [virtual keys](https://docs.litellm.ai/docs/proxy/virtual_keys), and [shared accounting contract](../../apps/vloer/docs/contracts/ploeg-execution.md).

## 11. Software result and repair cycle — intended

**Audience:** developers, reviewers, and leadership. **Question:** what should happen before human review? **State:** intended operating policy; a general automatic CI-to-ticket-to-fix loop is not yet demonstrated.

```mermaid
flowchart TB
    work["Workload in Ploeg<br/>Intended work record"] --> change["Agent produces change<br/>Execution step"]
    developer["Human-written change<br/>Enabled project"] --> ci
    change --> ci["CI runs agreed checks<br/>External system"]
    ci -->|Checks pass| review["Agent review<br/>Execution step"]
    ci -->|Checks fail| child["Create repair subticket<br/>External ticket system"]
    child --> repair["Repair within agreed limits<br/>Proposed execution policy"]
    repair --> change
    review -->|Changes needed| repair
    review -->|Ready with evidence| human["Person reviews and accepts<br/>Initial acceptance policy"]
    human -->|Changes needed| repair
    human -->|Accepted| release["Release through project process<br/>Separate responsibility"]
    release --> measure["Measure production result<br/>Separate responsibility"]
    repair -->|Limit reached or cause unclear| blocked["Ask for human help<br/>Retain failure evidence"]
```

The owner chose automatic repair subtickets in projects where the behavior is enabled, including human-written changes. How repeated failures map to existing subtickets, what parent to use when a change has no ticket, and what limits stop the loop remain open. The repair process must preserve the agreed check's meaning; weakening a check to obtain a green result cannot establish acceptance. Infrastructure failures and flaky checks need an explicit disposition as well as code defects. Human acceptance initially remains required, as agreed with the owner.

Today the ordinary Vloer path saves changes and review reports. Its additional trusted verifier requires Docker and is disabled in the Talos deployment. Automatic publication is disabled. Some Ploeg unattended paths have their own review and forge behavior; they do not establish this whole proposed loop. See [candidate delivery](../../apps/vloer/docs/contracts/candidate-delivery.md) and the [implementation differences](../../apps/vloer/docs/research/2026-09-11-ecosystem-implementation.md).

## 12. Homelab deployment — current, not a production reference architecture

**Audience:** CTO and operations. **Question:** where does each running part live? **State:** inspected cluster configuration at the start of this documentation exercise.

```mermaid
flowchart TB
    device["Developer device<br/>Deployment node<br/>Vloer browser"]
    subgraph cluster["Homelab Kubernetes cluster — deployment node"]
        gateway["Internal HTTP gateway<br/>Infrastructure routing"]
        auth["Authentik<br/>Identity provider"]
        subgraph controls["Namespace: ploeg"]
            v["Vloer rc.14<br/>1 server replica"]
            vd[("Vloer data volume<br/>SQLite and saved results")]
            p["Ploeg rc.6<br/>Controller"]
            pd[("Ploeg PostgreSQL")]
            worker["Ploeg worker Jobs<br/>Bronze paused for pilot"]
        end
        subgraph spaces["Namespace: de-vloer-workspaces"]
            wp["Workspace Pod<br/>OpenCode, relay, and tools"]
            disk[("Workspace volume<br/>Repository and native state")]
        end
        subgraph ai["Namespace: ai"]
            llm["LiteLLM gateway"]
        end
        keda["KEDA controller"]
        api["Kubernetes control plane"]
        flux["Flux and Helm controllers"]
    end
    deep["DeepSeek<br/>External provider<br/>Observed test route"]
    fire["Fireworks.ai<br/>External provider<br/>Intended, not configured here"]
    git["Deployment definitions in Git"]
    device -->|HTTPS| gateway
    gateway -->|Route application HTTPS| v
    device -->|Browser sign-in and consent| auth
    v -->|OIDC exchange and identity verification| auth
    v -->|Issue own authenticated session cookie| device
    v -->|Persist| vd
    v -->|Shared execution API| p
    p -->|Persist| pd
    v -->|Create direct workspace Pod| api
    api -->|Schedule| wp
    wp -->|Read and write files| disk
    wp -->|Outbound relay| v
    wp -->|Model requests| llm
    worker -->|Model requests| llm
    p -->|Manage execution keys| llm
    keda -->|Read pending-work count| pd
    keda -->|Create unattended Jobs| api
    api -->|Run configured Jobs| worker
    llm -->|Current configured route| deep
    llm -.->|Proposed provider connection| fire
    flux -->|Read desired deployment state| git
    flux -->|Reconcile deployments| api
```

The pilot permits one concurrent Vloer session and a maximum authorized budget of $0.25. These are test settings, not throughput measurements. The real provider test cost is provisional gateway accounting. Workspace isolation depends on the configured access, network, storage, and process restrictions. Sources: [homelab values](https://forgejo.webgrip.dev/webgrip/homelab-cluster/src/commit/8b2d069897415b3d42763966e94ef9b6eee16f83/kubernetes/apps/ploeg/de-vloer/app/helmrelease.yaml), [Ploeg values](https://forgejo.webgrip.dev/webgrip/homelab-cluster/src/commit/8b2d069897415b3d42763966e94ef9b6eee16f83/kubernetes/apps/ploeg/ploeg/app/helmrelease.yaml), and [recorded checks](index.md#how-to-read-the-evidence).

## 13. Possible consolidated containers — proposal, not a migration plan

**Audience:** CTO and product owner. **Question:** can both applications reuse execution machinery while local work stays independent of Ploeg? **State:** proposed implementation to compare against the existing paths.

```mermaid
flowchart TB
    human["Developer"]
    vloer["Vloer<br/>Sessions, intervention and review"]
    local["Standalone authorization<br/>Person's limits and local history"]
    ploeg["Ploeg<br/>Managed admission, scheduling and budgets"]
    tracker["External tracker"]
    runner["Candidate shared runner contract<br/>Workspace, harness, interruption and evidence"]
    harness["Harness in selected workspace"]
    model["Configured model access"]
    human --> vloer
    vloer -->|Standalone start| local
    vloer -->|Managed start| ploeg
    tracker -->|Assigned work| ploeg
    local -->|Explicit local authority| runner
    ploeg -->|Ploeg authority| runner
    runner --> harness
    harness --> model
```

The runner returns evidence to Vloer and reports results to Ploeg only for managed work. Its box represents possible shared behavior, not one service every execution must contact. A standalone installation has no Ploeg dependency. Each execution retains the authority selected at start; a failed Ploeg connection cannot switch it to local authority. Compare these paths before choosing a library, process protocol or extraction. The [transition proposal](../migration-proposal.md) defines the comparison and migration sequence.

## 14. Research and improvement — future behavior to define

**Audience:** product and leadership. **Question:** how does the broader ambition differ from coding automation? **State:** proposed future workflow.

```mermaid
flowchart LR
    question["Question or opportunity"] --> research["Research with cited evidence"]
    research --> decision["Evaluate business case"]
    decision -->|Do not build| stop["Accept supported stop conclusion"]
    decision -->|Worth testing| plan["Define experiment and success measure"]
    plan --> authorize["Authorize bounded work"]
    authorize --> execute["Build or run experiment"]
    execute --> measure["Measure actual result"]
    measure --> proposal["Propose next work"]
    proposal -.->|Execution authority still required| authorize
```

A research role, an implementation role, and a reviewer can share evidence without requiring an unrestricted agent chat network. The rules for creating follow-up tickets, approving spending, and accepting experiments remain to be defined. External telemetry must supply actual product outcomes; model output cannot stand in for market evidence. See [open questions](questions.md) and [bottleneck measurements](bottlenecks.md).
